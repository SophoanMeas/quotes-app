
<div class="col-12 col-md-4 col-lg-4 mb-3 d-flex">
  <div class="card h-100">
    <div class="card-body d-flex flex-column">
      <h5 class="card-header mb-1 text-center">{{category.category_name}}</h5>
      <p class="card-text text-center mt-3 mb-5">"{{description}}"</p>
      <div class="card-footer mt-auto  text-muted"> <h6 class="text-center">by - {{author}}</h6>
      <p class="text-center">posted by <a href="#" class="card-link">{{user.username}}</a></p>
      <i class="fa-regular fa-heart"></i></div>
    </div>
  </div>
</div>


