  <nav class="navbar navbar-light bg-light">
        <div class="container justify-content-around">
      <!-- QuoteSmith logo -->
        <div class="col-lg-3 col-sm-12 navbar-brand ">
          <img class="mw-100 img-fluid d-inline-block mb-0 " src="/images/QuoteSmith-Horizontal.png" alt="QuoteSmith Logo">
        </div>
      <!-- Search bar and radio buttons -->
      <div class="col-3 ">
        <div class="input-group mb-3">
          <input type="text" class="form-control" placeholder="Search quotes" aria-label="Search quotes" aria-describedby="basic-addon2">
          <div class="input-group-append">
            <button id="search-btn" class="btn btn-outline-primary" type="button">Search</button>
          </div>
        </div>  
        <!-- radio buttons -->
          <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" name="inlineRadioOptions" id="inlineRadio1" value="option1">
            <label class="form-check-label" for="inlineRadio1">by Author</label>
          </div>
          <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" name="inlineRadioOptions" id="inlineRadio2" value="option2">
            <label class="form-check-label" for="inlineRadio2">by Keyword</label>
          </div>
        </div> 
     
   <div class="col">
        <span class="d-flex justify-content-end">
          <!-- Login -->
      
      {{#if loggedIn}}
            <i class="user-icon fa-solid fa-circle-user fa-2x"></i><p class="username text-left fa-solid">{{username}}</p> &nbsp; 
            {{/if}}
          <div class="text-right">
            <button type="button" class="btn btn-outline-primary" data-toggle="modal" data-target="#loginModal">Login</a>
          </div>
            &nbsp;
            {{#if loggedIn}}
            <div class="text-right">
            <button type="button" id="logout" class="btn btn-outline-primary" data-toggle="modal" data-target="">Logout</a>
          </div>
  <script src="./js/logout.js"></script>
          {{/if}}

          <!-- Sign up -->
          &nbsp;
            {{#if loggedIn}}
          {{else}}
             <div class="text-right">
            <button type="button" class="btn btn-outline-primary" data-toggle="modal" data-target="#signUpModal">Sign Up</button>
          </div>
          {{/if}}
        </span>
      </div>

        </div>  
      </nav> 

 <!--LOGIN Modal -->
  {{>loginModal}}
  {{>signupModal}}

