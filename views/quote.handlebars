<section class="container">
  <h3 class="Quotes">{{quote.description}}</h3>
  <section class="flex-row justify-center">
    {{#each quote.description as |quote|}}
    {{> description}}
    {{/each}}
  </section>
</section>
